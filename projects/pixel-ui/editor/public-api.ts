/**
 * Preferred import path: `import { … } from 'pixel-ui/editor'` (workspace tsconfig).
 * TipTap peers are optional — install them when using the editor.
 * Primary `pixel-ui` barrel re-exports the same surface until ng-packagr secondary FESM works.
 */
export * from '../src/lib/pixel-editor/public-api';
