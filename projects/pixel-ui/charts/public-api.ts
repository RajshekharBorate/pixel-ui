/**
 * Preferred import path: `import { … } from 'pixel-ui/charts'` (workspace tsconfig).
 * ECharts is an optional peer — install it when using chart facades.
 * Primary `pixel-ui` barrel re-exports the same surface until ng-packagr secondary FESM works.
 */
export * from '../src/lib/pixel-chart/public-api';
