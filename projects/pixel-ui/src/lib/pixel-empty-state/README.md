# pixel-empty-state

Designed placeholder for no-data, no-results, and first-use states: icon or illustration, heading, description, and an actions row.

## Overview

- Never leave an emptied region blank — pixel-empty-state standardizes the icon + heading + description + actions anatomy.
- Use the icon input for Material Symbols, or project [pixelEmptyStateMedia] for custom illustrations; [pixelEmptyStateActions] hosts recovery actions.
- announce opts into role="status" + aria-live="polite" for empty outcomes that appear after async searches or filtering.

## Use cases

- No search/filter results with a reset action
- First-use onboarding states for empty workspaces
- Table, list, and card bodies with no data

<!-- API-CONTRACT:START — generated by tools/generate-readme-api.mjs. Do NOT edit between these markers; run `npm run readme:api` instead. -->

## API contract

_Machine-generated from the component source. This is the behavioral API surface: any change
to it is a **breaking-change candidate** and must be deliberate. After modifying this
component, run `npm run readme:api` and review this section's diff as a regression check._

### Component `pixel-empty-state` (`PixelEmptyStateComponent`)

Designed placeholder for regions with nothing to show — no data yet, no search results, an empty filter outcome, or a first-use state. Renders an icon (or a custom `[pixelEmptyStateMedia]` illustration), a heading, a description, and an optional `[pixelEmptyStateActions]` row, so consumers never leave a blank region.

**Inputs**

| Input | Type | Default | Description |
| --- | --- | --- | --- |
| `id` | `string` | `''` | Optional element id applied to the host. Stable id for `aria-describedby` references from the emptied region. |
| `icon` | `string` | `''` | Material Symbols ligature rendered as the visual. Decorative (`aria-hidden`); skipped when empty or when `[pixelEmptyStateMedia]` content is projected alongside. |
| `heading` | `string` | `''` | Short headline stating what is empty. Keep it factual ("No results"), not apologetic. |
| `description` | `string` | `''` | Supporting copy explaining why and what to do next. Projected default-slot content renders below this when both are present. |
| `size` | `PixelEmptyStateSize` | `'md'` | Density preset scaling icon, type, and spacing. `sm` suits table/list bodies, `lg` suits full-page states. |
| `align` | `PixelEmptyStateAlign` | `'center'` | Horizontal alignment of the content stack. `start` reads better inside dense list/table containers. |
| `announce` | `boolean` | `false` | Announces the empty state to assistive technology when it appears. Adds `role="status"` + `aria-live="polite"` — enable for empty states that replace content after async searches or filtering, not for static first-render states. |

### Exported types

| Type | Definition |
| --- | --- |
| `PixelEmptyStateSize` | `'sm' | 'md' | 'lg'` |
| `PixelEmptyStateAlign` | `'start' | 'center'` |

<!-- API-CONTRACT:END -->

## Behavior notes

- **Anatomy parts render only when non-empty**: icon/heading/description are skipped for
  blank inputs; the media, body, and actions slot wrappers collapse via `:empty`. An
  empty-state with no inputs and no projected content renders nothing visible.
- **Media vs icon**: `[pixelEmptyStateMedia]` (custom illustration) and `icon` can coexist —
  media renders above the icon — but by convention supply only one.
- **Announcement is opt-in**: by default the component is inert content (no role). Set
  `announce` ONLY for empty states that replace content dynamically (search/filter wiped a
  list) — `role="status"` + `aria-live="polite"` then announce the change. Static first-render
  empty states must not announce.
- **No skeleton state by design**: an empty state is the *outcome* after loading; show
  `pixel-skeleton`/`pixel-loader` while loading, then either data or this component.
- Description text is capped at `36rem` measure for readability; the component always fills
  its container inline-size (`align` controls the stack alignment, not the box width).
- Below the library `sm` breakpoint (600px), the actions row stacks full-width for touch-friendly
  taps on narrow viewports.

## Accessibility

- The icon is decorative (aria-hidden); the heading carries the meaning.
- Static empty states are plain content; set announce for post-filter outcomes so screen readers hear the change.
- Actions are regular projected buttons/links and keep their own semantics.

## Theme customization

- Component tokens: --pixel-empty-state-color, --pixel-empty-state-muted-color, --pixel-empty-state-icon-color, --pixel-empty-state-icon-size, --pixel-empty-state-heading-size, --pixel-empty-state-gap, --pixel-empty-state-padding.
- Sizes sm/md/lg scale icon, heading, and padding from the shared scales.

## Breaking changes

- None recorded since initial release.
