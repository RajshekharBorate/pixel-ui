# Requirement

Create an operations dashboard using Pixel UI: KPI cards with sparklines, recent activity data grid, loading and empty states, responsive token-only styling.

Workflow: PAGE (docs playground). Golden fixture for Phase 2.
