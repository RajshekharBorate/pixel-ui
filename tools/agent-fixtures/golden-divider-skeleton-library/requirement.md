# Requirement

LIBRARY extension: add `showSkeleton` to `pixel-divider` so loading layouts can reserve a thin rule footprint without inventing a parallel placeholder. Include docs example, tests, and contract sync.

Workflow: LIBRARY (Phase 3 dry-run). Auto-approve composition.
