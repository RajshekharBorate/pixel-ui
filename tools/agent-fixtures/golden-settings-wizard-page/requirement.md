# Requirement

Create a workspace settings wizard using Pixel UI: linear stepper with profile fields (input + select), notification toggles, review step, loading skeleton, finish confirmation.

Workflow: PAGE (docs playground). Golden fixture for Phase 2.
